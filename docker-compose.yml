services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: githire
      POSTGRES_PASSWORD: githire
      POSTGRES_DB: githire
    ports:
      - "5432:5432"
    volumes:
      - githire_pg:/var/lib/postgresql/data

volumes:
  githire_pg:
